# Test-only Postgres fixture for integration and Alembic tests
# Usage: docker compose -f docker/docker-compose.test.yml up -d

name: data-agent-test

services:
  postgres-test:
    image: postgres:16
    container_name: aeroflow-postgres-test
    ports:
      - "5433:5432"
    environment:
      POSTGRES_DB: aeroflow_test
      POSTGRES_HOST_AUTH_METHOD: trust
    tmpfs:
      - /var/lib/postgresql/data
    deploy:
      resources:
        limits:
          cpus: "1.00"
          memory: 512M
