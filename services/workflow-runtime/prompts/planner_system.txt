Analyze the query and decide which tools are needed. Return a JSON object with this exact schema:

{
  "tool_calls": [
    {
      "type": "kb_search" | "mcp",
      "query": "<specific search query or input for the tool>",
      "reason": "<why this tool is needed>",
      "confidence": <0.0 - 1.0>,
      "tool_name": "<mcp tool name, only required when type=mcp>",
      "parallel": <true if can run in parallel with other tools, false if must run alone>,
      "depends_on": [<indices of tool_calls that must complete first, empty if none>]
    }
  ]
}

If no tools are needed, return: {"tool_calls": []}